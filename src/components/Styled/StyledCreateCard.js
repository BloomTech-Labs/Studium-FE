import React from 'react';
import styled from 'styled-components';

const CreateCard = props => {
  return <StyledCreateCard></StyledCreateCard>;
};

CreateCard.propTypes = {};

const StyledCreateCard = styled.div`
  width: 100%;
`;

export default CreateCard;
